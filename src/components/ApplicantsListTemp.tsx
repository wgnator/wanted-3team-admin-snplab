import { searchQueryType } from '../interfaces/types';

type ApplicantsListProps = {
  query: searchQueryType | null;
};
export function ApplicantsList({ query }: ApplicantsListProps) {
  return <>{}</>;
}
